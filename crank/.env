# RPC da Solana (Vamos usar Localnet primeiro, depois mudamos pra Devnet/Mainnet)
RPC_URL=http://127.0.0.1:8899

# A chave privada do nosso bot (Crank). Vamos colocar a key real aqui depois.
CRANK_SECRET_KEY=